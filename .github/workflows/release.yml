name: Release
on:
  push:
    tags: ["v*"]

jobs:
  test:
    uses: ./.github/workflows/test.yml

  build:
    needs: test
    runs-on: ubuntu-latest
    permissions:
      contents: write # needed to create the release and upload assets
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
          - goos: linux
            goarch: arm64
          - goos: freebsd
            goarch: amd64
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: false

      - name: Build binary
        env:
          CGO_ENABLED: 0
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          # Strip the leading "v" so the version number (e.g. 0.1.0) is
          # used for the binary version string and asset filename, while
          # the git tag itself remains v-prefixed.
          VERSION="${GITHUB_REF_NAME#v}"
          OUT="go-mail-form_${VERSION}_${GOOS}_${GOARCH}"
          go build -trimpath -ldflags="-s -w -X main.version=${VERSION}" -o "${OUT}" .
          tar -czf "${OUT}.tar.gz" "${OUT}"

      - name: Upload to release
        uses: softprops/action-gh-release@v3
        with:
          files: "*.tar.gz"
